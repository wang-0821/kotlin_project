plugins {
    id "org.flywaydb.flyway" version "$flywayVersion"
}

dependencies {
    compile libs['mybatis']
    compile libs['HikariCP']
    compile libs['flyway-core']
    compile libs['mysql-connector-java']
    compile project(":kotlin-base")
}